#include <stdlib.h>
#include <stdio.h>
#include <assert.h>

/**
 * @brief add
 * 
 * @param[in] a : a value
 * @param[in] b : b value
 * @param[out] sum : sum of a and b value
 * @retval 0 : success
 */
int my_int_add( int * a, int * b, int * sum );
